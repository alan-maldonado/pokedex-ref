services:
  backend:
    build: ./backend
    volumes:
      - ./backend/data:/app/data
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "4001:80"
    depends_on:
      - backend
    restart: unless-stopped
